apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chat
  template:
    metadata:
      labels:
        app: chat
    spec:
      containers:
      - name: chat
        image: 100xdevs/chat:0b79dfbd268f3fa81aa74bd4e318dc26756ca47b
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: REDIS_URL
          value: "redis://redis.default.svc.cluster.local:6379"
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: chat
spec:
  selector:
    app: chat
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
