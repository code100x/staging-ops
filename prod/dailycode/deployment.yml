apiVersion: apps/v1
kind: Deployment
metadata:
  name: dailycode
  labels:
    app: dailycode
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dailycode
  template:
    metadata:
      labels:
        app: dailycode
    spec:
      containers:
        - name: dailycode
          image: 100xdevs/dailycode:e6b189ffc39bff8812e96238aecc3bd7ef11ac58
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          volumeMounts:
            - name: secret
              mountPath: /usr/src/app/.env
              subPath: .env
      imagePullSecrets:
        - name: regcred
      volumes:
        - name: secret
          secret:
            secretName: dailycode-secret
            items:
              - key: .env
                path: .env
