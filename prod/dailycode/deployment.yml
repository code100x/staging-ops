apiVersion: apps/v1
kind: Deployment
metadata:
  name: dailycode
  labels:
    app: dailycode
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dailycode
  template:
    metadata:
      labels:
        app: dailycode
    spec:
      containers:
        - name: dailycode
          image: 100xdevs/dailycode:d9ca1af2a18eb9195ad6f5feae5c4a0e4d0f7969
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          volumeMounts:
            - name: secret
              mountPath: /usr/src/app/.env
              subPath: .env
      imagePullSecrets:
        - name: regcred
      volumes:
        - name: secret
          secret:
            secretName: dailycode-secret
            items:
              - key: .env
                path: .env
