apiVersion: apps/v1
kind: Deployment
metadata:
  name: job-board-staging
  labels:
    app: job-board-staging
spec:
  replicas: 2
  selector:
    matchLabels:
      app: job-board-staging
  template:
    metadata:
      labels:
        app: job-board-staging
    spec:
      containers:
        - name: job-board-staging
          image: 100xdevs/job-board-staging:2271c5742f4dd3b3831bb129bc136ed30437101a
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          volumeMounts:
            - name: secret
              mountPath: /usr/src/app/.env
              subPath: .env
      imagePullSecrets:
        - name: regcred
      volumes:
        - name: secret
          secret:
            secretName: job-board-staging-secret
            items:
              - key: .env
                path: .env
