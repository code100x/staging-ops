apiVersion: apps/v1
kind: Deployment
metadata:
  name: algo-sweeper
  labels:
    app: algo-sweeper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: algo-sweeper
  template:
    metadata:
      labels:
        app: algo-sweeper
    spec:
      containers:
        - name: algo-sweeper
          image: 100xdevs/algo-sweeper:f24e1266da4b4ec5e0eafdfaed7c4ff9e640f04a
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          volumeMounts:
            - name: secret
              mountPath: /usr/src/app/apps/sweeper/.env
              subPath: .env
      imagePullSecrets:
        - name: regcred
      volumes:
        - name: secret
          secret:
            secretName: algo-arena-secret
            items:
              - key: .env
                path: .env
