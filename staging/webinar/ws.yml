apiVersion: apps/v1
kind: Deployment
metadata:
  name: webinar-staging-ws
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webinar-staging-ws
  template:
    metadata:
      labels:
        app: webinar-staging-ws
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
        - name: webinar-staging-ws
          image: 100xdevs/webinar-ws-staging:21392fe02ef8709f5d5679c6dbd9d78749564644
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: REDIS_URL
              value: "redis://redis-staging.default.svc.cluster.local:6379"
---
apiVersion: v1
kind: Service
metadata:
  name: webinar-staging-ws
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: webinar-staging-ws
